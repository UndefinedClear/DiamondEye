# GoldenEye Async Load Tool

## Описание

Этот проект — асинхронный инструмент нагрузочного тестирования HTTP‑серверов.  
Он запускает несколько «воркеров» (роботов), которые параллельно отправляют HTTP‑запросы на указанный адрес, поддерживает разные режимы нагрузки (flood, extreme, slowloris), фаззинг путей/методов/заголовков и собирает статистику (RPS).

> Важно: используйте только на своих серверах или с явного разрешения.  
> Любое несанкционированное применение может быть незаконным.

---

## Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt

(или установите вручную `httpx`, `asyncio`, `matplotlib` — если нужен график.)

### Пример запуска

python main.py http://127.0.0.1:8000 --http2 --flood -w 10 -s 500

Где:

- `http://127.0.0.1:8000` — целевой URL.
- `--http2` — использовать HTTP/2.
- `--flood` — режим максимальной скорости.
- `-w 10` / `--workers 10` — количество асинхронных воркеров.
- `-s 500` / `--sockets 500` — лимит сокетов/соединений.
```

---

## Как это работает (по шагам)

### 1. Запуск и аргументы командной строки

Точка входа — `main.py`.  
При запуске, например:

```bash
python main.py http://127.0.0.1:8000 --http2 --flood -w 10 -s 500

аргументы разбираются в модуле `args.py`:
```
```python
def parse_args():
    parser = argparse.ArgumentParser()
    parser.add_argument('url')
    parser.add_argument('-w', '--workers', type=int, default=10)
    parser.add_argument('-s', '--sockets', type=int, default=500)
    parser.addargument('--http2', action='storetrue')
    parser.addargument('--flood', action='storetrue')
    # другие флаги...
    return parser.parse_args()
```
В итоге получается объект `args`, например:

```python
args.url = "http://127.0.0.1:8000"
args.workers = 10
args.sockets = 500
args.http2 = True
args.flood = True
```
Программа «знает», куда стучаться, сколько воркеров запускать и какие режимы включать.

---

### 2. Создание объекта атаки

В `main.py` создаётся основной объект:

```python
attack = GoldenEyeAttack(
    url=args.url,
    workers=args.workers,
    sockets=args.sockets,
    use_http2=args.http2,
    flood=args.flood,
    # другие параметры...
)
```
`GoldenEyeAttack` — это «менеджер атаки», который:

- хранит настройки (URL, количество воркеров, режимы);
- управляет запуском и остановкой воркеров;
- собирает статистику;
- формирует отчёты.

---

### 3. Запуск воркеров

Метод `start()` в `attack.py`:

```python
async def start(self):
    base_kwargs = {
        "http2": self.use_http2,
        "verify": not self.nosslcheck,
        "timeout": httpx.Timeout(10.0),
    }

    tasks = [
        asyncio.create task(self.worker(base kwargs))
        for   in range(self.workers)
    ]
    await asyncio.gather(*tasks)
```
Что происходит:

- формируются общие настройки HTTP‑клиента (`base_kwargs`);
- создаётся `self.workers` асинхронных задач (воркеров);
- все воркеры запускаются параллельно и работают до остановки.

---

### 4. Логика одного воркера

Упрощённый вид:

```python
async def worker(self, base_kwargs):
    client = httpx.AsyncClient(**base_kwargs)
    await client.aenter()  # открыть клиент
while not self.shutdownevent.is_set():
        # 1. Случайный URL
        url = self.buildrandomurl()

        # 2. Случайные заголовки
        headers = generate_headers(self.host, self.useragents, self.referers)

        # 3. Тело запроса (если задан размер)
        data = 'X' * self.datasize if self.datasize > 0 else None

        # 4. Отправка запроса
        await client.request("GET", url, headers=headers, content=data)

        # 5. Пауза
        if self.flood:
            await asyncio.sleep(0.001)  # почти без паузы
        else:
            await asyncio.sleep(random.uniform(0.01, 0.1))
```

По сути, воркер:

1. Открывает HTTP‑клиент.

2. В цикле:
   - генерирует URL;
   - генерирует заголовки;
   - при необходимости формирует тело нужного размера;
   - отправляет запрос;
   - делает небольшую паузу (или почти не делает её в режиме `--flood`).

---

### 5. Режимы нагрузки: `--flood` и `--extreme`

#### Flood

- Используются постоянные соединения.
- На одном соединении отправляется много запросов подряд.
- Меньше накладных расходов → выше RPS (запросов в секунду).

#### Extreme

Идея в коде:

```python
if self.extreme:
    async with httpx.AsyncClient(**basekwargs) as clientonce:
        await self.sendrequest(clientonce, method, url, headers, data)
else:
    await self.send_request(client, method, url, headers, data)
```

- В режиме `--extreme`:
  - для *каждого* запроса создаётся новый клиент → новое TCP/SSL‑соединение.
- Это:
  - тяжелее для сервера (много рукопожатий);
  - но даёт меньше RPS из‑за накладных расходов.

---

### 6. Slowloris (медленные запросы)

Иногда (по вероятности `slow_rate`) запускается медленный запрос:

```python
if random.random() < self.slow_rate:
    task = asyncio.createtask(self.slowrequest(url))
```

`slow_request`:

1. Открывает TCP‑соединение вручную:

```python
reader, writer = await asyncio.open_connection(host, port, ssl=...)
```

2. Отправляет начало HTTP‑запроса:

http
GET / HTTP/1.1
Host: example.com

3. Очень медленно досылает заголовки:

```python
while ...:
    writer.write(b"X-Header: value\r\n")
    await writer.drain()
    await asyncio.sleep(0.1)
```

Сервер считает, что клиент ещё не закончил запрос, держит соединение открытым и тратит ресурсы.  
Много таких «висящих» соединений могут забить пул соединений/воркеров на сервере.

---

### 7. Фаззинг (случайные данные)

Фаззинг — это подстановка случайных/нестандартных данных, чтобы нагрузить разные части сервера.

#### 7.1. Случайные пути (`--path-fuzz`)


```python
def buildrandomurl(self):
    path = "/news"  # базовый путь
    if self.path_fuzz:
        path += "/" + "/".join(randomstring(8) for  in range(3))
        # пример: /news/abc12345/xyz67890/qwe09876
    return self.base_url + path + "?t=123"
```
- Без `--path-fuzz`: путь фиксированный (`/news`).
- С `--path-fuzz`: добавляются случайные сегменты пути.

#### 7.2. Случайные методы (`--method-fuzz`)


```python
self.fuzz_methods = ['PROPFIND', 'REPORT', 'LOCK']
if self.method_fuzz and random.random() < 0.3:
    method = random.choice(self.fuzz_methods)
```
- Иногда вместо стандартного `GET` выбирается один из «редких» методов:
- `PROPFIND`, `REPORT`, `LOCK` и т.п.

#### 7.3. Случайные заголовки (`--junk`, `--header-flood`)

```python
def generateheaders(..., usejunk=False, header_flood=False):
    if use_junk:
        count = 20 if header_flood else 5
        for _ in range(count):
            headers[f'X-{random string(5)}'] = random_string(10)
```

- `--junk` — добавляет несколько случайных заголовков `X-XXXXX: YYYYYYYYYY`.
- `--header-flood` — увеличивает их количество (например, до 20).

---

### 8. Статистика и RPS

#### Сбор статистики


```python
async def collectrpsstats(self):
    last = 0
    while not self.shutdownevent.is_set():
        await asyncio.sleep(1.0)
        now = self.sent - last
        self.rps_history.append({'time': time.time(), 'rps': now})
        last = self.sent
- `self.sent` — общее количество отправленных запросов.
- Каждую секунду:
  - вычисляется, сколько запросов добавилось;
  - это значение сохраняется как RPS за эту секунду.
```

#### Вывод в консоль


```python
def print_stats(self):
    elapsed = time.time() - self.start_time
    rps = int(self.sent / (elapsed + 1))
    print(f"\r📬 Sent: {self.sent} | 🚀 RPS: {rps} | ⏱️ {int(elapsed)}s", end="")
```
На экране отображается:

- общее количество отправленных запросов;
- примерный RPS;
- время работы в секундах.

---

### 9. Остановка (Ctrl+C)

При нажатии `Ctrl+C` срабатывает обработчик сигнала:

```python
def signal_handler():
    attack.shutdownevent.set()  # даёт сигнал всем воркерам

loop.addsignalhandler(signal.SIGINT, signal_handler)
```
Воркеры в своём цикле проверяют:

```python
while not self.shutdownevent.is_set():
    # отправка запросов
```
- Пока флаг не установлен — воркер продолжает работу.
- После `set()` — воркер выходит из цикла и корректно завершается.
- Так все воркеры останавливаются аккуратно, без «обрыва» посередине.
---

### 10. Отчёты

После остановки атаки могут быть вызваны:

- `generate_report()` — текстовый отчёт (количество запросов, средний RPS и т.д.).
- `save_json_report()` — сохранение статистики в JSON‑файл.
- `save_plot()` — построение графика RPS по времени (если установлен `matplotlib`).

Это позволяет:

- анализировать поведение сервера под нагрузкой;
- сравнивать разные запуски между собой.

## 11. Воркеры и сокеты
```markdown

Воркер (`worker`) — это **асинхронная задача**, которая непрерывно отправляет HTTP-запросы на целевой сервер.  
Каждый воркер работает **независимо**, и их количество задаётся флагом `-w`:
```

```bash
python main.py http://127.0.0.1:8000 -w 10
```
→ Будет запущено 10 воркеров.

🔁 Что делает один воркер?
Создаёт HTTP-клиент (httpx.AsyncClient)
В бесконечном цикле:
Генерирует случайный URL (/news?t=12345)
Формирует случайные заголовки (User-Agent, X-Junk, и др.)
Генерирует тело (если указано --data-size)
Отправляет запрос
Делает небольшую задержку
Повторяет, пока атака не остановлена
⚙️ Режимы работы
| Режим | Поведение | |------|----------| | Обычный | Один клиент → много запросов (keep-alive / HTTP/2) | | --extreme | Новое TCP-соединение на каждый запрос → высокая нагрузка на стек |

🛑 Остановка
При нажатии Ctrl+C:

Сигнал ловится в main.py
Срабатывает attack._shutdown_event.set()
Все воркеры проверяют этот флаг в цикле:
```python
while not self._shutdown_event.is_set():
```
Цикл завершается → воркер корректно останавливается
🔐 Потокобезопасность
Чтобы несколько воркеров не "сломали" счётчики:

Используется asyncio.Lock()
Обновление sent и failed происходит в защищённой секции:
```python
async with self.lock:
    self.sent += 1
```


---

## Кратко (для понимания)

1. Ты запускаешь `main.py` с нужными параметрами.

2. Программа читает аргументы и создаёт объект атаки.

3. Объект атаки запускает несколько асинхронных воркеров.

4. Каждый воркер бесконечно шлёт HTTP‑запросы (с разными режимами и случайными данными).

5. Параллельно собирается статистика (RPS).

6. По `Ctrl+C` все воркеры корректно останавливаются.

7. В конце можно сохранить отчёты и графики.

---

## Лицензия

(Укажи здесь свою лицензию: MIT, GPL, BSD или другую, если нужно.)