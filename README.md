# DiamondEye v6.7 — Professional HTTP Load Tester

⚠️ ВАЖНО: Используйте ТОЛЬКО для тестирования систем, 
которые вам принадлежат или на которые у вас есть письменное разрешение.  
Несанкционированная атака — нарушение закона.

💎 DiamondEye — это инструмент для:
- Нагрузочного тестирования веб-серверов
- Проверки устойчивости к DDoS
- Образования и изучения кибербезопасности
- Пентеста (с разрешения)

❌ Не используйте против:
- Государственных ресурсов
- Социальных сетей
- Банков
- Облачных платформ без разрешения

==================================================

## 🔧 КОМАНДЫ

Запуск:
  python main.py <URL> [OPTIONS]

Пример:
  python main.py https://127.0.0.1:8443 --http2 --junk --flood -w 500 -s 2000 -l report

==================================================

## ⚡ ДОСТУПНЫЕ ФЛАГИ

-w, --workers <число>
    Количество параллельных воркеров
    По умолчанию: 10
    Пример: -w 500

-s, --sockets <число>
    Количество сокетов на воркер
    Общее соединений = workers × sockets
    Пример: -s 1000

-m, --methods <GET,POST,PUT,PATCH,ALL>
    HTTP-методы для использования
    Пример: --methods GET,POST
    Или: --methods ALL

-u, --useragents <файл>
    Путь к файлу с User-Agent строками, по одной на строку

-n, --no-ssl-check
    Отключает проверку SSL-сертификатов
    Полезно для тестов на localhost

--proxy <прокси>
    Использует HTTP/HTTPS прокси
    Пример: --proxy http://127.0.0.1:8080

-d, --debug
    Включает вывод ошибок в консоль
    Полезно при отладке

--http2
    Включает HTTP/2 (требует: pip install httpx[http2])
    ❌ Несовместим с --extreme

--junk
    Добавляет случайные заголовки X-* в каждый запрос

--random-host
    Генерирует случайные поддомены в заголовке Host
    Пример: abc123.example.com

--slow <float>
    Доля запросов, которые будут "медленными" (Slowloris)
    Пример: --slow 0.05 → 5% медленных запросов

--extreme
    Каждый запрос — новое TCP-соединение
    ❌ Отключает HTTP/2, но усиливает нагрузку на TCP-стек

--flood
    Минимальные задержки между запросами → максимум RPS
    Работает лучше всего с --http2

--data-size <размер>
    Размер тела POST/PUT/PROPFIND и др.
    Поддерживаемые форматы: 1k, 64k, 1m, 4m
    Пример: --data-size 64k

--path-fuzz
    Генерирует случайные глубокие пути
    Пример: /a/b/c/d/e/file.php

--header-flood
    До 20 мусорных заголовков (работает с --junk)

--method-fuzz
    Использует редкие HTTP-методы: PROPFIND, REPORT, LOCK, UNLOCK
    Полезно для теста WebDAV

-l, --log <файл>
    Сохраняет текстовый отчёт о тесте

--json <файл>
    Сохраняет JSON-отчёт с детальной статистикой

--plot <файл>
    Сохраняет график RPS (требуется matplotlib)

==================================================

## 📌 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

1. Максимальный RPS (HTTP/2 + flood)
   python main.py https://target.com \
     --http2 --junk --flood --path-fuzz \
     -w 500 -s 2000

2. Экстремальная нагрузка на TCP-стек
   python main.py https://target.com \
     --extreme --flood --junk \
     --data-size 64k \
     -w 200 -s 2000

3. Полный фаззинг (макс. воздействие)
   python main.py https://127.0.0.1:8000 \
     --http2 --junk --header-flood --random-host \
     --path-fuzz --method-fuzz \
     --data-size 1m --flood \
     -w 100 -s 1000

4. Тест с отчётом
   python main.py https://test.local \
     --http2 -w 100 -s 500 \
     -l report.log --json report.json --plot rps.png


## 🛡️ ПРЕДУПРЕЖДЕНИЯ

==================================================

• --http2 и --extreme — НЕСОВМЕСТИМЫ
• --proxy не работает с --slow (Slowloris идет в обход прокси)
• --data-size применяется только к POST, PUT, PATCH, PROPFIND и др.
• Избегайте -w 10000 -s 10000 — это может убить систему
• Используйте --flood осторожно — может вызвать перегрев CPU

## ⚙️ ТРЕБОВАНИЯ К СИСТЕМЕ

- Linux / macOS / Windows (частично)
- Лимит файловых дескрипторов: 65536
- Диапазон портов: 1024–65535
- `tcp_tw_reuse=1`, `somaxconn=65535`

👉 Смотрите `SYSTEM.md` для настройки.

==================================================

## 📦 УСТАНОВКА НУЖНЫХ МОДУЛЕЙ
```bash
pip install --force-reinstall requirements.txt
```
==================================================

## 📊 ВЫХОДНЫЕ ДАННЫЕ

Во время работы:
  📬 Sent: 12500 | ⚠️ Failed: 3 | 🚀 RPS: 1250 | ⏱️ 10s

Форматы отчётов:
- Текстовый: детали нагрузки, статус сервера
- JSON: можно парсить скриптами
- График: визуализация RPS

## 🔐 БЕЗОПАСНОСТЬ И ЭТИКА

Этот инструмент — двойного назначения.
Он может быть использован как во благо, так и во вред.

Вы несёте полную ответственность за его использование.

Разработчик не несёт ответственности за:
- Несанкционированные атаки
- Ущерб третьим лицам
- Нарушение законов

==================================================

MIT License | Created and Enhanced by larion and Neo
